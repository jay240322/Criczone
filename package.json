{
    "name": "criczone-root",
    "version": "1.0.0",
    "description": "Root script to run frontend and backend",
    "scripts": {
        "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
        "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend"
    },
    "dependencies": {
        "concurrently": "^8.2.0"
    },
    "author": "",
    "license": "ISC"
}